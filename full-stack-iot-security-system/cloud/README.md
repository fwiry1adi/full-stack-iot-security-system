# cloud/

Incident logging + push notification integration (Objective 7) —
scheduled for Weeks 7-8.

Decision pending: Firebase (Firestore/Storage + Cloud Messaging) vs. AWS
(S3 + DynamoDB + SNS) vs. a lightweight custom backend hitting APNs
directly. Whichever gets picked shapes how incident records are structured,
so decide before Week 7 if possible.
